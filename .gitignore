# Environment variables / secrets - never commit personal info or credentials
.env

# Python virtual environment - huge, machine-specific, everyone regenerates their own
venv/

# The actual database file - this is DATA, not code; also grows constantly and would bloat the repo
osrs_data.db

# Python cache files - auto-generated bytecode, not source code
__pycache__/
*.pyc

explore.py